# [260217]《4-1 求根问题》diff（初版 -> 最终版）

对照范围：

- 初版：`note/ch4/4-1-求根问题.md`（取自 git commit `8b099c3`）
- 最终版：`note/ch4/4-1-求根问题.md`（工作区文件）

diff 写法约定（对齐 `..\\AGENTS.md` 的 "可复核 / 低噪声 / NO GUESSING" ）：

- 只展示 "发生变化的行 + 少量上下文" （unified diff），避免把整篇重贴造成噪声。
- 解释与复盘写在 diff code block 之外；diff block 保持可直接复制对照。
- 为避免 CRLF/LF 造成伪差异，生成 diff 前先按文本层面统一换行。

## 修改复盘（逐点对应 diff hunks）

1. **#1 段落：把 "求根=解方程" 与 "迭代近似" 的主线说得更直接**
 - 把 "把方程写成 $f(x)=0$ 的形式足够一般" 改写为 "求根问题也可以看作解方程" ，降低抽象度。
 - 将 "构造近似序列" 这句加粗，并去掉括号式预告，让读者先抓住核心计算策略：构造并推进收敛序列。

2. **#2 段落：从 "条件性" 统一为 "条件数" ，并把动机拆成可扫读的要点**
 - 标题与术语统一为 "条件数" ，与后文 $\kappa_r$ 记号对齐。
 - 把 "为什么可以扰动一个函数" 的两点理由改成列表，并对 "数据/结果/绝对条件数" 做加粗，减少读者猜测（NO GUESSING）。

3. **定理后的旁注：用反函数视角解释 $\kappa_r=|f^{\prime}(r)|^{-1}$ 的几何意义**
 - 在 "等价地…" 之后补了 `> **Note?**`：把竖向常数扰动 $\epsilon$ 转成 $f^{-1}$ 的输入扰动，从而把条件数理解为 $|(f^{-1})^{\prime}(0)|$。
 - 写作逻辑：把公式落到 "曲线越平 → 竖向扰动引起更大横向位移" 的直觉上，便于读者在图像上理解。

4. **#3 段落：残差/反向误差部分改成 "先说明为什么需要它，再给定义" **
 - 先交代 "误差需要真值、但真值常不知道" ，因此需要残差来判断是否接受近似根。
   - 定义与术语格式（加粗、句末点号）做了统一。

5. **反向误差的解释方式调整：从代数构造改为 "残差=反向误差" + 图示**
   - 初版用 $g(x)=f(x)-f(\tilde r)$ 的构造说明 backward error；最终版改为更直觉的叙述，并用配图辅助理解。
 - 写作逻辑：把推导负担从读者转移到图示与口头解释上，更贴合 "降低门槛" 的笔记目标。

6. **#4 多重根：把 "根很近导致敏感" 单独分流为 Note**
 - 将 "根太近导致敏感" 的提醒从主线句子中拆出来放到 `> **Note?**`，避免主线段落一次承载过多信息。

## Unified Diff（raw）

```diff
--- note/ch4/4-1-求根问题.md
+++ note/ch4/4-1-求根问题.md
@@ -8,12 +8,12 @@
 
 我们先聚焦于单变量单函数的 **rootfinding problem**.
 
-> **Definition:** Rootfinding problem
+> **Definition:** **Rootfinding problem.** 
 > Given a continuous scalar function $f$ of a scalar variable, find a real number $r$, called a **root**, such that $f(r)=0$.
 
-把方程写成 $f(x)=0$ 的形式足够一般：如果我们想解 $g(x)=h(x)$，只要令 $f=g-h$，然后去找 $f$ 的根即可.
+求根问题 $f(x)=0$ 也可以看作解方程，如果我们想解 $g(x)=h(x)$，只要令 $f=g-h$，然后去找 $f$ 的根即可.
 
-与前面章节的线性问题不同，这里常见的情况是：即使在精确算术下，根也无法用有限次运算直接写出来. 因此我们通常构造一个近似序列，让它形式上收敛到根，并在某一项看起来 "足够好" 时停止 (后面会更明确地说明什么叫 "足够好" ).
+与前面章节的线性问题不同，这里常见的情况是：即使在精确算术下，根也无法用有限次运算直接写出来. 因此我们通常 **构造一个近似序列，让这个序列收敛到根**，并在某一项看起来 "足够好" 时停止.
 
 在 Python 生态中，`scipy.optimize` 提供了通用的求根例程，例如 `scipy.optimize.root` (可用于向量变量) 以及标量专用的 `scipy.optimize.root_scalar`.
 
@@ -57,21 +57,22 @@
 > ```
 > After plotting, we can use the rough locations of the zeros as initial guesses, then refine them with a solver.
 
-> **Note?** 这里用 `root` 是为了呼应 "通用求根器通常以向量变量为接口" 这一点，因此目标函数需要返回长度为 1 的数组.
+**#2 条件数：根对函数扰动的敏感性**
 
-**#2 条件性：根对函数扰动的敏感性**
+在求根问题里，**"数据"** 是一个连续函数 $f$，**"结果"** 是它的一个根. 我们关心的是：当 $f$ 发生扰动时，根会如何变化？这里我们会计算 **绝对条件数**，而不是相对条件数.
 
-在求根问题里，"数据" 是一个连续函数 $f$，"结果" 是它的一个根. 我们关心的是：当 $f$ 发生扰动时，根会如何变化？这里我们会计算绝对条件数，而不是相对条件数.
+把函数当作数据去扰动，可能一开始看起来有点抽象. 但至少有两点是合理的：
 
-把函数当作数据去扰动，可能一开始看起来有点抽象. 但至少有两点使它变得合理：第一，$f$ 的取值会用浮点数表示，必然有舍入误差；第二，在许多应用中，$f$ 本身可能不是一个显式公式，而是某个数值过程的输出，也会引入误差.
+- 第一，$f$ 的取值会用浮点数表示，必然有舍入误差.
+- 第二，在许多应用中，$f$ 本身可能不是一个显式公式，而是某个数值过程的输出，也会引入误差.
 
-虽然函数的扰动方式有无穷多种，但只考虑 "常数扰动" 就足够抓住关键现象. 假设 $f$ 在某个根 $r$ 附近至少有一阶连续导数. 把 $f$ 扰动为
+虽然函数的扰动方式有很多种，但只考虑 "常数扰动" 就足够抓住关键现象. 假设 $f$ 在某个根 $r$ 附近至少有一阶连续导数. 把 $f$ 扰动为
 
 $$
 \tilde f(x)=f(x)+\epsilon,
 $$
 
-则对应的根 (若仍存在) 会从 $r$ 变为 $\tilde r=r+\delta$，满足 $\tilde f(\tilde r)=0$. 我们定义绝对条件数 $\kappa_r$ 为 $\left|\frac{\delta}{\epsilon}\right|$ 在 $\epsilon\to 0$ 时的极限.
+则对应的根 (若仍存在) 也会被扰动，从 $r$ 变为 $\tilde r=r+\delta$，以满足 $\tilde f(\tilde r)=0$. 我们定义绝对条件数 $\kappa_r$ 为 $\left|\frac{\delta}{\epsilon}\right|$ 在 $\epsilon\to 0$ 时的极限.
 
 由 Taylor 展开，
 
@@ -81,7 +82,7 @@ $$
 
 由于 $f(r)=0$，就能把 $\delta$ 与 $\epsilon$ 联系起来，从而得到条件数.
 
-> **Theorem:** Condition number of rootfinding
+> **Theorem:** **Condition number of rootfinding.** 
 > If $f$ is differentiable at a root $r$, then the absolute condition number of $r$ with respect to constant changes in $f$ is
 > $$
 > \kappa_r = \bigl| f'(r) \bigr|^{-1}.
@@ -90,32 +91,51 @@ $$
 
 等价地，上式就是 $f^{-1}$ 在 0 处导数的绝对值.
 
+> **Note?** 这个等价关系可以从 "反函数看扰动" 的角度理解得更直接. 由于 $\tilde f(x)=f(x)+\epsilon$，其根 $\tilde r$ 满足
+> $$
+> 0=\tilde f(\tilde r)=f(\tilde r)+\epsilon
+> \quad\Longleftrightarrow\quad
+> f(\tilde r)=-\epsilon.
+> $$
+> 若在 $r$ 附近 $f$ 存在局部反函数 (例如 $f'(r)\neq 0$，由反函数定理可知)，则
+> $$
+> r=f^{-1}(0),\qquad \tilde r=f^{-1}(-\epsilon),
+> $$
+> 从而
+> $$
+> \delta=\tilde r-r=f^{-1}(-\epsilon)-f^{-1}(0)\approx (f^{-1})'(0)\cdot(-\epsilon).
+> $$
+> 因此 $\left|\frac{\delta}{\epsilon}\right|\approx |(f^{-1})'(0)|$. 再由 $(f^{-1})'(0)=1/f'(r)$，就得到 $\kappa_r=|f'(r)|^{-1}$. 几何上，$|f'(r)|$ 越小，曲线在交点处越 "平"，同样的竖向扰动 $\epsilon$ 会引起更大的横向位移 $\delta$，根也就越敏感.
+
+下面的例子帮助我们感受条件数的影响.
+
 > **Demo:** Well-conditioned vs poorly-conditioned roots.
+>
 > ```Python
 > import numpy as np
 > import matplotlib.pyplot as plt
->
+> 
 > interval = (0.8, 1.2)
 > x = np.linspace(*interval, 600)
 > eps = 0.03
->
+> 
 > def ribbon_plot(f, title):
->     y = f(x)
->     plt.fill_between(x, y - eps, y + eps, alpha=0.25)
->     plt.plot(x, y)
->     plt.scatter([1.0], [0.0])
->     plt.axhline(0.0, color="k", lw=1)
->     plt.gca().set_aspect("equal", adjustable="box")
->     plt.xlabel("x")
->     plt.ylabel("f(x)")
->     plt.title(title)
->     plt.ylim(-0.2, 0.2)
->
+>  y = f(x)
+>  plt.fill_between(x, y - eps, y + eps, alpha=0.25)
+>  plt.plot(x, y)
+>  plt.scatter([1.0], [0.0])
+>  plt.axhline(0.0, color="k", lw=1)
+>  plt.gca().set_aspect("equal", adjustable="box")
+>  plt.xlabel("x")
+>  plt.ylabel("f(x)")
+>  plt.title(title)
+>  plt.ylim(-0.2, 0.2)
+> 
 > # Well-conditioned near r=1: f'(1) = -1.
 > f1 = lambda x: (x - 1.0) * (x - 2.0)
 > ribbon_plot(f1, "Well-conditioned root")
 > plt.show()
->
+> 
 > # Poorly-conditioned near r=1: f'(1) = -0.01.
 > f2 = lambda x: (x - 1.0) * (x - 1.01)
 > ribbon_plot(f2, "Poorly-conditioned root")
@@ -123,39 +143,36 @@ $$
 > ```
 > The vertical thickness is the same in both plots, but the possible horizontal displacement of the root can be dramatically larger when $|f'(r)|$ is small.
 
-当 $|f'(r)|$ 很小的时候，我们必须接受：计算出来的根近似值，可能无法做到很小的正向误差. 因为误差需要 "真值" 才能计算，我们还需要一个能直接测量的量.
+当 $|f'(r)|$ 很小的时候 (条件数大)，函数曲线在根附近几乎是平的，我们很难进一步减小误差.
 
 **#3 残差与反向误差**
 
-> **Definition:** Rootfinding residual
-> If $\tilde{r}$ approximates a root $r$ of function $f$, then the **residual** at $\tilde{r}$ is $f(\tilde{r})$.
+由于误差需要 "真值" 才能计算，但是求根问题常常不知道真值，所以我们除了误差之外还需要一个量，来判断我们是否能接受计算出来的根.
 
-直觉上，残差小往往意味着误差也小. 为了把关系说清楚，令 $\tilde r$ 近似 $f$ 的根 $r$，并定义新函数
+> **Definition:** **Rootfinding residual.**
+> If $\tilde{r}$ approximates a root $r$ of function $f$, then the **residual** at $\tilde{r}$ is $f(\tilde{r})$.
 
-$$
-g(x)=f(x)-f(\tilde r).
-$$
+直觉上，残差小往往意味着误差也小，原因在于 **求根问题的残差就是反向误差**，而反向误差小意味着算法稳定.
 
-显然 $g(\tilde r)=0$，也就是说 $\tilde r$ 是 $g$ 的一个真根. 同时，$g(x)-f(x)$ 恰好等于常数 $f(\tilde r)$，因此残差刻画了：我们距离一个 "被精确求解" 的求根问题有多远.
+至于为什么求根问题的残差就是反向误差，我们可以回顾 **1-4-稳定性** 中的反向误差相关内容，再参考下图的推导.![4-1-求根问题的反向误差](./.assets/4-1-求根问题的反向误差.jpg)
 
 > **Observation:** The backward error in a root estimate is equal to the residual.
 
-一般来说，如果 $\kappa_r$ 很大，我们不该期待根近似有很小的正向误差. 但残差可以直接估计反向误差，从而作为停止准则的重要参考.
+一般来说，如果 $\kappa_r$ 很大，我们不该期待根近似有很小的正向误差. 但残差可以直接估计反向误差，从而作为 **是否能接受计算出来的根** 的重要参考.
 
 **#4 多重根**
 
-由 $\kappa_r=\bigl|f'(r)\bigr|^{-1}$ 自然会想到：如果在根 $r$ 处有 $f'(r)=0$ 会怎样？下面的定义把多项式的代数重数推广到了更一般的可微函数.
+由 $\kappa_r=\bigl|f'(r)\bigr|^{-1}$ 自然会想，如果在根 $r$ 处有 $f'(r)=0$ 会怎样？下面的定义把多项式的代数重数推广到了更一般的可微函数.
 
-> **Definition:** Multiplicity of a root
+> **Definition:** **Multiplicity of a root.**
 > If $f(r)=f'(r)=\cdots=f^{(m-1)}(r)=0$, but $f^{(m)}(r)\neq 0$, then we say $f$ has a root of **multiplicity** $m$ at $r$. In particular, if $f(r)=0$ and $f'(r)\neq 0$, then $m=1$ and we call $r$ a **simple root**.
 
-另一个等价刻画是：存在可微函数 $q$，且 $q(r)\neq 0$，使得
+另一个等价刻画是，存在可微函数 $q$，且 $q(r)\neq 0$，使得
 
 $$
 f(x)=(x-r)^m q(x).
 $$
 
-当 $r$ 不是简单根时，上面的条件数等价意义下会变成无穷大. 即使 $r$ 是简单根，如果 $|f'(r)|$ 非常小，求根也会变得困难：这通常意味着存在另一个根非常接近 $r$，使得 $q(r)\approx 0$. 在 **1-4-稳定性** 的多项式求根例子里，我们已经见过 "根太近会导致敏感性很强" 这一现象.
-
-> **Note?** 所谓 "条件数等效无穷大"，可以理解为：当 $f$ 被大小为 $\epsilon$ 的扰动改变时，根的相对变化不再是 $O(\epsilon)$ (当 $\epsilon\to 0$ 时).
+当 $r$ 不是简单根时，上面的条件数等价意义下会变成无穷大. 即使 $r$ 是简单根，如果 $|f'(r)|$ 非常小，求根也会变得困难，这通常意味着存在另一个根非常接近 $r$，使得 $q(r)\approx 0$.
 
+> **Note?** 在 **1-4-稳定性** 的多项式求根例子里，我们已经见过 "根太近会导致敏感性很强" 这一现象.
```
