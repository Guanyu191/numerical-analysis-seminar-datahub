# [260215]《3-2 正规方程》diff（初版 -> 最终版）

对照范围：

- 初版：`note/ch3/3-2-法方程.md`（取自 git commit `8b099c3`）
- 最终版：`note/ch3/3-2-正规方程.md`（工作区文件）

我这次把 diff 文档按 "可复核、低噪声、可逐行追溯" 的原则重写（结合 `..\\AGENTS.md` 的主线/旁注分流与 NO GUESSING）：

- **只展示变化**：用 unified diff（`---` / `+++` / `@@`）展示 "发生变化的行 + 少量上下文" ，避免整篇重贴造成噪声。
- **避免伪差异**：先按文本层面统一换行（LF）再 diff，避免 CRLF/LF 导致的 "看似全篇都变了" 。
- **解释不混进 diff**：复盘/解释写在 code block 之外，保证 diff block 可直接复制对照。

## 修改复盘（逐点对应 diff hunks）

下面每一点都能在后面的 diff 里找到对应的 `-` / `+` 修改行。

1. **#1（最小二乘问题陈述与证明前导）**
 - 统一了个别标点（例如把中文冒号 `：` 调整为半角 `:`），并把 "在证明里" 改为 "在接下来的证明里" ，让读者更明确：接下来进入证明段落。
 - 写作逻辑：这是 "节奏与一致性" 的微调，不改变数学内容，但降低阅读摩擦。

2. **#2（几何解释段落重写：目标 -> 路径 -> 正交）**
 - 初版用一句话直接给出 "Ax 在列空间 + 残差正交" 的结论；最终版改为先重申优化目标 `min ||b - A x||_2`，再引入 "路径" `b - A x` 的几何直觉，并把 "垂直于" 句子加粗作为读者的视觉锚点。
 - 你把 "更一般的陈述" （残差与 A 的列空间正交）分流到 `> **Note?**`：主线段落只推进一个论证点，泛化放到旁注里（符合 `..\\AGENTS.md` 的 "主线优先、旁注分流" ）。

3. **#3（伪逆、可逆条件与计算实践）**
 - "过定问题" 改成 "超定问题" ，术语更常见、更稳定。
 - 把 "当 A 的列向量线性无关时，A^T A 可逆" 加粗：这等价于把后文会用到的前提显式摆出来，避免读者靠上下文去猜（NO GUESSING）。
 - 在 "计算角度" 的建议里对两句话加粗（很少显式算 A^+；更常见把正规方程当线性系统来解），把 "概念表达" 和 "实践建议" 清晰分层。

4. **#3 定理（A^T A 的结构性质）补 proof，并标明边界**
 - 最终版补上了该定理第 2/3 点的证明，同时明确 "第一部分留作习题" 。
 - 写作逻辑：既避免无端扩写（把不该展开的部分留在习题），也避免把关键结论变成 "只给结论不给依据" 。

5. **#4/#5（算法段落与稳定性段落的术语/强调统一）**
 - "条件数" 统一为 "条件数" ，与后文 `kappa(A)` 的定义对齐，术语链条更顺。
 - 把 "条件数平方化是正规方程不稳定的根源" 加粗：让读者把这一节的核心因果结论抓牢。

## Unified Diff（raw）

```diff
--- note/ch3/3-2-法方程.md
+++ note/ch3/3-2-正规方程.md
@@ -6,7 +6,7 @@
 
 **#1 正规方程与最小二乘解**
 
-我们现在要解 **3-1-用函数拟合数据** 里提出的线性最小二乘问题：给定 $\mathbf{A}\in\mathbb{R}^{m\times n}$ 与 $\mathbf{b}\in\mathbb{R}^{m}$ (且 $m>n$)，找到 $\mathbf{x}\in\mathbb{R}^{n}$ 使得
+我们现在要解 **3-1-用函数拟合数据** 里提出的线性最小二乘问题: 给定 $\mathbf{A}\in\mathbb{R}^{m\times n}$ 与 $\mathbf{b}\in\mathbb{R}^{m}$ (且 $m>n$)，找到 $\mathbf{x}\in\mathbb{R}^{n}$ 使得
 
 $$
 \|\mathbf{b}-\mathbf{A}\mathbf{x}\|_2
@@ -14,7 +14,7 @@
 
 尽可能小.
 
-这里有一个简洁的刻画. 在证明里，我们会用到下面这个代数恒等式：对任意向量 $\mathbf{u},\mathbf{v}$，
+这里有一个简洁的刻画. 在接下来的证明里，我们会用到下面这个代数恒等式: 对任意向量 $\mathbf{u},\mathbf{v}$，
 
 $$
 \begin{aligned}
@@ -65,9 +65,11 @@
 
 **#2 几何解释**
 
-正规方程有一个很直观的几何解释. $\mathbf{A}\mathbf{x}$ 一定落在 $\mathbf{A}$ 的列空间里. 在所有这种向量里，离 $\mathbf{b}$ 最近的那个，会让残差向量 $\mathbf{A}\mathbf{x}-\mathbf{b}$ 与列空间正交.
-
-因此，对任意 $\mathbf{z}$，我们都应有
+正规方程有一个很直观的几何解释. 再次回顾我们的目标，是 "找一个 $\mathbf{x}$ 使得 $\|\mathbf{b}-\mathbf{A}\mathbf{x}\|_2$ 尽可能地小"，即 $\mathbf{A}\mathbf{x}$ 与 $\mathbf{b}$ 尽量靠近 (默认在 2-norm 的度量下). 其中 $\mathbf{A}\mathbf{x}$ 一定落在 $\mathbf{A}$ 的列空间里，且 $\mathbf{A}\mathbf{x}$ 要抵达 $\mathbf{b}$ 则必须经过 "路径" $\mathbf{b}-\mathbf{A}\mathbf{x}$. 此时如果想让 $\mathbf{b}-\mathbf{A}\mathbf{x}$ 最小，那么必须使 **$\mathbf{b}-\mathbf{A}\mathbf{x}$ "垂直于" $\mathbf{A}\mathbf{x}$ (详见教材的图)**.
+
+> **Note?** 更一般地，我们所希望的是 $\mathbf{b}-\mathbf{A}\mathbf{x}$ 与 $\mathbf{A}$ 的列空间正交.
+
+因此，对任意 $\mathbf{z}$，我们都应该有
 
 $$
 (\mathbf{A}\mathbf{z})^{T}(\mathbf{A}\mathbf{x}-\mathbf{b})=0.
@@ -83,9 +85,9 @@
 (\mathbf{A}^{T}\mathbf{A})\mathbf{x}=\mathbf{A}^{T}\mathbf{b},
 $$
 
-我们就把过定问题转成了一个 $n\times n$ 的方阵线性系统.
-
-当 $\mathbf{A}$ 的列向量线性无关时，$\mathbf{A}^{T}\mathbf{A}$ 可逆. 这时我们可以把解写成一个更紧凑的形式.
+我们就把超定问题转成了一个 $n\times n$ 的方阵线性系统.
+
+**当 $\mathbf{A}$ 的列向量线性无关时，$\mathbf{A}^{T}\mathbf{A}$ 可逆.** 这时我们可以把解写成一个更紧凑的形式.
 
 > **Definition:** Pseudoinverse
 > Let $\mathbf{A}\in\mathbb{R}^{m\times n}$ with $m>n$. If $\mathbf{A}^{T}\mathbf{A}$ is nonsingular, then the pseudoinverse of $\mathbf{A}$ is the $n\times m$ matrix
@@ -99,7 +101,7 @@
 \mathbf{x}=\mathbf{A}^{+}\mathbf{b}.
 $$
 
-从计算角度看，我们很少会直接去算 $\mathbf{A}^{+}$ (就像我们很少直接去算矩阵逆一样). 在 Python 里也可以用 `np.linalg.pinv` 直接计算伪逆，但和显式求逆一样，这在实践中很少必要. 更常见的做法是把正规方程当作线性系统来解.
+从计算角度看，我们 **很少会直接去算 $\mathbf{A}^{+}$** (就像我们很少直接去算矩阵逆一样). 在 Python 里也可以用 `np.linalg.pinv` 直接计算伪逆，但和显式求逆一样，这在实践中没必要. 更常见的做法是 **把正规方程当作线性系统来解**.
 
 矩阵 $\mathbf{A}^{T}\mathbf{A}$ 还有一些重要的结构性质.
 
@@ -107,17 +109,30 @@
 > 1. $\mathbf{A}^{T}\mathbf{A}$ is symmetric.
 > 2. $\mathbf{A}^{T}\mathbf{A}$ is singular if and only if the columns of $\mathbf{A}$ are linearly dependent. (Equivalently, if and only if the rank of $\mathbf{A}$ is less than $n$.)
 > 3. If $\mathbf{A}^{T}\mathbf{A}$ is nonsingular, then it is positive definite.
+>
+> **Proof:** The first part is left to the exercises. For the second part, suppose that
+> $\mathbf{A}^T\mathbf{A}\mathbf{z}=\boldsymbol{0}$. Note that $\mathbf{A}^T\mathbf{A}$ is singular if and only if $\mathbf{z}$ may be nonzero. Left-multiplying by $\mathbf{z}^T$, we find that
+> $$
+> 0 = \mathbf{z}^T\mathbf{A}^T\mathbf{A}\mathbf{z}=(\mathbf{A}\mathbf{z})^T(\mathbf{A}\mathbf{z}) = \| \mathbf{A}\mathbf{z} \|_2^2,
+> $$
+> which is equivalent to $\mathbf{A}\mathbf{z}=\boldsymbol{0}$. Thus $\mathbf{z}$ may be nonzero if and only if the columns of $\mathbf{A}$ are linearly dependent.
+>
+> Finally, for any nonzero $n$-vector $\mathbf{v}$ we have
+> $$
+> \mathbf{v}^T(\mathbf{A}^T\mathbf{A})\mathbf{v}=\| \mathbf{A}\mathbf{v} \|_2^2\ge 0,
+> $$
+> and equality is not possible when $\mathbf{A}^T\mathbf{A}$ is nonsingular, thanks to the second part. Hence in that case $\mathbf{A}^T\mathbf{A}$ is positive definite.
 
 **#4 实现：用正规方程解最小二乘**
 
-直接根据正规方程求解最小二乘问题的步骤可以概括为：
+直接根据正规方程求解最小二乘问题的步骤可以概括为: 
 
 > **Algorithm:** Solution of linear least squares by the normal equations
 > 1. Compute $\mathbf{N}=\mathbf{A}^{T}\mathbf{A}$.
 > 2. Compute $\mathbf{z}=\mathbf{A}^{T}\mathbf{b}$.
 > 3. Solve the $n\times n$ linear system $\mathbf{N}\mathbf{x}=\mathbf{z}$ for $\mathbf{x}$.
 
-在这一步里，我们可以利用 **2-9-利用矩阵结构** 里的 Cholesky 分解：当 $\mathbf{N}$ 对称正定时，用 Cholesky 分解来解线性系统会更合适.
+在这一步里，我们可以利用 **2-9-利用矩阵结构** 里的 Cholesky 分解，当 $\mathbf{N}$ 对称正定时，用 Cholesky 分解来解线性系统会更合适.
 
 > **Demo:** Solving least squares via the normal equations (Cholesky).
 > ```Python
@@ -148,7 +163,7 @@
 
 在实际计算中，我们经常直接用线性最小二乘求解器 (例如 Python 的 `np.linalg.lstsq`). 这类求解器通常不会通过正规方程来计算解，因为正规方程可能不稳定.
 
-线性最小二乘问题的条件数，刻画的是解 $\mathbf{x}$ 对数据 $\mathbf{A},\mathbf{b}$ 的敏感性. 为了讨论条件数，我们先把 "矩阵条件数" 的定义推广到非方阵.
+线性最小二乘问题的条件数，刻画的是解 $\mathbf{x}$ 对数据 $\mathbf{A},\mathbf{b}$ 的敏感性. 为了讨论条件数，我们先把 "矩阵条件数" 的定义推广到非方阵.
 
 > **Definition:** Matrix condition number (rectangular case)
 > Let $\mathbf{A}\in\mathbb{R}^{m\times n}$ with $m>n$. Define
@@ -157,11 +172,11 @@
 > $$
 > If the rank of $\mathbf{A}$ is less than $n$ (i.e., if it has linearly dependent columns), then $\kappa(\mathbf{A})=\infty$.
 
-如果最小残差 $\|\mathbf{b}-\mathbf{A}\mathbf{x}\|_2$ 相对较小，那么线性最小二乘问题的条件数会接近 $\kappa(\mathbf{A})$.
+如果最小残差 $\|\mathbf{b}-\mathbf{A}\mathbf{x}\|_2$ 相对较小，那么线性最小二乘问题的条件数会接近 $\kappa(\mathbf{A})$.
 
 但作为算法，正规方程会先构造 $n\times n$ 的线性系统 $(\mathbf{A}^{T}\mathbf{A})\mathbf{x}=\mathbf{A}^{T}\mathbf{b}$. 当我们解这个系统时，数据扰动会被放大到大约 $\kappa(\mathbf{A}^{T}\mathbf{A})$ 的量级.
 
-下面的恒等式说明：正规方程会把条件数平方化，这正是它不稳定的根源.
+下面的恒等式说明：**正规方程会把条件数平方化，这正是它不稳定的根源.**
 
 > **Theorem:** Condition number in the normal equations
 > Let $\mathbf{A}\in\mathbb{R}^{m\times n}$ with $m>n$. Then
```
